# Spring properties
spring:
  application:
     name: search-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

# HTTP Server
server:
  port: 8000   # HTTP (Tomcat) port
