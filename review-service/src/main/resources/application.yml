# Spring properties
spring:
  application:
     name: review-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

# HTTP Server
server:
  port: 9999   # HTTP (Tomcat) port
